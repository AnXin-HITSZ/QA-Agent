# 环境变量(含密钥),绝不入库;保留示例
.env
.env.*
!.env.example

# TLS 证书 / 私钥,绝不入库(阿里云签发的 nginx 证书放在这里)
certs/
*.key
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
*.egg-info/
.mypy_cache/
.ruff_cache/

# Node / 前端(后续引入)
node_modules/
dist/
*.local

# OS / IDE
.DS_Store
.idea/
.vscode/
